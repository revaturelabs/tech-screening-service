spring:
  application:
    name: tech-screening-service
  cloud:
    config:
      uri: http://ec2-54-89-224-207.compute-1.amazonaws.com:8888 # ${CALIBER_CONFIG_SERVER_URL}
      fail-fast: true
 # H2
  jpa:
    hibernate:
      ddl-auto: create-drop
eureka:
  client:
     enabled: false