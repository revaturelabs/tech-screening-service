spring:
  application:
    name: tech-screening-service
  cloud:
    config:
      enabled: false
# H2
  jpa:
    hibernate:
      ddl-auto: create-drop
eureka:
  client:
     enabled: false
     
server:
  port: 8184